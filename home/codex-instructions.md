# Response annotations

When a user message contains response annotations, address every annotation by
its annotation number. Under each `Annotation N` label, quote the exact
annotated response text as a Markdown blockquote, then give the answer. Do this
even when an annotation has no written comment. Quote the annotated response
text, not the user's annotation comment.
